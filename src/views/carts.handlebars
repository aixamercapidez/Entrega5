{{!-- <div>
    {{#each carts}}
        <li>
           
        
<strong>cart id: </strong>{{this._id}} -

            <strong>products: </strong>{{this.Products}} -
            
            
        </li>
      
    {{/each}}

</div> --}}

<h1>Su carrito {{cart._id}} 🛒</h1>

{{#if cartsProducts}}
  <section class="cartContainer">
    {{#each cartsProducts}}
      <div class="cartProduct">
        <h3>Nombre: {{Products.idProduct.title}}</h3>
        <h4>Precio:{{idProduct.price}}</h4>
        {{!-- <img class="img" src="{{product.thumbnail}}" /> --}}
        <h4>Cantidad: {{quantity}}</h4>
        <div>
          <button class="plusButton" id="plusOne">+</button>
          <button class="minusButton" id="minusOne">-</button>
        </div>
      </div>
    {{/each}}
    <h2><a class="finish" href="">Terminar compra</a></h2>
  </section>
{{else}}
  <h3>No hay productos agregados!</h3>
{{/if}}